import React from 'react'
import Button from 'react-bootstrap/Button'

function ButtonComponent (props) {
    return (
        <>
            <Button 
                style={{

                }}
                variant = 'outline-secondary'>Download output csv file</Button>{' '}
        </>
    );
}

export default ButtonComponent